{
  "name": "spektra-projects",
  "version": "1.0.0",
  "private": true,
  "description": "Spektra Projects - Deployable applications workspace",
  "scripts": {
    "dev": "pnpm --filter ./baseline dev",
    "build": "pnpm --filter ./baseline build",
    "dev:baseline": "pnpm --filter ./baseline dev",
    "build:baseline": "pnpm --filter ./baseline build"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
