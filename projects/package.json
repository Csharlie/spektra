{
  "name": "spektra-projects",
  "version": "1.0.0",
  "private": true,
  "description": "Spektra Projects - Deployable applications workspace",
  "scripts": {
    "dev": "pnpm --filter ./bellator dev",
    "build": "pnpm --filter ./bellator build",
    "dev:bellator": "pnpm --filter ./bellator dev",
    "build:bellator": "pnpm --filter ./bellator build"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
